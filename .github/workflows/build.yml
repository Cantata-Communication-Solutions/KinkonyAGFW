name: Build Firmware

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - 'KinkonyAGFW_Arduino/**'
  pull_request:
    branches: [ "main", "master" ]
    paths:
      - 'KinkonyAGFW_Arduino/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v2.0.0

      - name: Install ESP32 core
        run: |
          arduino-cli core update-index --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          arduino-cli core install esp32:esp32 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

      - name: Install Libraries
        run: |
          arduino-cli lib install "WebSockets"
          arduino-cli lib install "ArduinoJson"
          arduino-cli lib install "rc-switch"

      - name: Compile Sketch
        run: |
          arduino-cli compile --fqbn esp32:esp32:esp32 KinkonyAGFW_Arduino/Haptique_AGFW.ino

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: compiled-firmware
          path: KinkonyAGFW_Arduino/build/esp32.esp32.esp32/Haptique_AGFW.ino.bin
          if-no-files-found: ignore
